{
  "name": "xycdev-blog-prototype",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 4321",
    "build": "node scripts/build-timeline.mjs",
    "timeline:build": "node scripts/build-timeline.mjs",
    "timeline:add": "node scripts/timeline-add.mjs",
    "timeline:publish": "node scripts/publish-timeline.mjs",
    "check": "node scripts/check.mjs",
    "deploy": "npm run build && npm run check && npx wrangler pages deploy . --project-name xycdev-journal"
  }
}
